# API Integration

Agent Zero exposes real-time APIs and tools for integration.

- WebSocket server for live updates
- FastAPI endpoints: `/status`, `/metrics`, `/health`
- MCP server/client support
- A2A (Agent-to-Agent) protocol

Example: use `call_subordinate` to orchestrate multi-agent workflows.
