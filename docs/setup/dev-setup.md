# Development Setup

Instructions for developers working on Agent Zero core.

- Use Python 3.11+
- Install dev dependencies: `pip install -r a0/requirements.txt`
- Run tests: `pytest tests/`
- Hot reload: use `python a0/agent.py` with `DEBUG` environment variable.

Refer to Architecture docs for component details.
